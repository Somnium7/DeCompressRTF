{
  "name": "@kenjiuno/decompressrtf",
  "version": "1.0.0",
  "description": "Decompress CompressedRTF (used in outlook msg)",
  "author": "kenjiuno",
  "repository": "github:HiraokaHyperTools/DeCompressRTF",
  "license": "BSD-2-Clause",
  "private": false,
  "type": "module",
  "scripts": {
    "build": "node ./build.mjs",
    "test": "node --test --experimental-test-coverage --enable-source-maps \"./lib/**/*.test.mjs\"",
    "prepare": "npm run build && npm test"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^7.0.0"
  },
  "exports": {
    ".": {
      "require": {
        "default": "./lib/index.cjs",
        "types": "./lib/index.d.cts"
      },
      "import": {
        "default": "./lib/index.mjs",
        "types": "./lib/index.d.mts"
      }
    }
  }
}
